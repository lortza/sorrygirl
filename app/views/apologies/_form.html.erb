<%= form_for(@apology) do |f| %>
  <% if @apology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apology.errors.count, "error") %> prohibited this apology from being saved:</h2>

      <ul>
      <% @apology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>Hey Girl, write your ideal apology note here: (35 words max)</p>
  <div class="field">
    <%= f.text_area :body, autofocus: true, cols: 40, rows: 7 %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-sm btn-primary' %>
  </div>
<% end %>
